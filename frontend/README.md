# Sjenjak Frontend

Reconstructed Create React App project from the production JavaScript and CSS
source maps.

## Commands

```bash
npm install
npm start
npm run build
```

The generated output is written to `frontend/build`. The original production
build at the repository root remains unchanged.
